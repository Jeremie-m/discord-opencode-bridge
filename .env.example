# ===========================================
# Discord-OpenCode Bridge Configuration
# ===========================================

# Discord Bot Configuration (REQUIRED)
# Get these from https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here
DISCORD_GUILD_ID=your_guild_id_here

# Channel prefix for OpenCode sessions (optional)
# Only channels starting with this prefix will be handled
# Default: "oc-"
DISCORD_CHANNEL_PREFIX=oc-

# OpenCode Server Configuration
# URL where `opencode serve` is running
OPENCODE_SERVER_URL=http://localhost:4096

# Default project path for sessions
# If channel name doesn't specify a project, this path is used
OPENCODE_DEFAULT_PROJECT_PATH=D:/Dev

# Timeout for OpenCode requests in milliseconds (default: 5 minutes)
# Increase if you have long-running tasks
OPENCODE_TIMEOUT=300000

# Logging level: debug, info, warn, error
LOG_LEVEL=info
