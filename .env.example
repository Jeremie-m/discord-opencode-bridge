# ===========================================
# Discord-OpenCode Bridge Configuration
# ===========================================

# Discord Bot Configuration (REQUIRED)
# Get these from https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here
DISCORD_GUILD_ID=your_guild_id_here

# Channel prefix for OpenCode sessions (optional)
# Only channels starting with this prefix will be handled
# Default: "oc-"
DISCORD_CHANNEL_PREFIX=oc-

# OpenCode Server Configuration
# URL where `opencode serve` is running
# Use 127.0.0.1 instead of localhost on Windows
OPENCODE_SERVER_URL=http://127.0.0.1:4096

# REQUIRED: Working directory for OpenCode
# This is where OpenCode will run and look for your projects
# Example: C:/Users/YourName/Projects or D:/Dev
OPENCODE_DEFAULT_PROJECT_PATH=

# Timeout for OpenCode requests in milliseconds (default: 10 minutes)
# Increase if you have long-running tasks (ultrawork mode needs more time)
OPENCODE_TIMEOUT=600000

# Logging level: debug, info, warn, error
LOG_LEVEL=info
